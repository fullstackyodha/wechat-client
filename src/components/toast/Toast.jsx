import React from "react";
import "@/components/toast/Toast.scss";
import PropTypes from "prop-types";

function Toast(props) {
    const { toastLife, position } = props;

    return <div>Toast</div>;
}

Toast.PropTypes = {};
export default Toast;
